all_services:
  - name: [service-name]
          tags: [tags]
          # whether env file is required
          has_env: [true|false]
          # whether bulding of Dockerfile is required
          has_build: [true|false] 
          